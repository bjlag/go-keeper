env: dev

address:
    host: localhost
    port: 8080

auth:
    accessTokenExp: 5m
    refreshTokenExp: 720h # 30 days
    secretKey: secret

database:
    host: localhost
    port: 5444
    name: master
    user: postgres
    password: secret